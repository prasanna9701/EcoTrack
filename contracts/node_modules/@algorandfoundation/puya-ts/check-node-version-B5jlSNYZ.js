import chalk from 'chalk';
import { C as Constants } from './options-_3gbKwvv.js';
import { p as parseSemVer } from './resolve-puya-path-ORzGtNzH.js';

function writeError(text) {
    // eslint-disable-next-line no-console
    console.error(chalk.red(text));
}
function checkNodeVersion() {
    const min = parseSemVer(Constants.minNodeVersion);
    const actual = parseSemVer(process.versions.node);
    if (actual.major < min.major ||
        (actual.major === min.major && actual.minor < min.minor) ||
        (actual.major === min.major && actual.minor === min.minor && actual.rev < min.rev)) {
        writeError(`Installed node version ${process.versions.node} is older than the minimum required version ${Constants.minNodeVersion}. You may experience compatibility issues.`);
    }
}

export { checkNodeVersion as c };
//# sourceMappingURL=check-node-version-B5jlSNYZ.js.map
