import type * as lsp from 'vscode-languageserver/node';
import type { LogEvent } from '../index';
import { LogLevel } from '../index';
import type { LogSink } from './index';
export declare class LanguageServerLogSink implements LogSink {
    private connection;
    constructor(connection: lsp.Connection);
    minLogLevel: LogLevel;
    add(e: LogEvent): void;
}
