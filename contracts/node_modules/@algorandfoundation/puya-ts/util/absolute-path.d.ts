export declare class AbsolutePath {
    private value;
    constructor(value: string);
    static resolve({ path, workingDirectory }: {
        path: string;
        workingDirectory?: AbsolutePath;
    }): AbsolutePath;
    toString(): string;
    basename(ext?: string): string;
    replaceAll(searchValue: string, replaceValue: string): AbsolutePath;
    join(...paths: string[]): AbsolutePath;
    equals(other: AbsolutePath | string): boolean;
    relativeTo(other: AbsolutePath | string): string;
    /**
     * Resolve the specified relative path relative to this absolute path
     * @param relativePath
     */
    resolve(relativePath: string): AbsolutePath;
}
