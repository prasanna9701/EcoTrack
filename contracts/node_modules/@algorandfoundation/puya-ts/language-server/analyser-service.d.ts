import type * as lsp from 'vscode-languageserver';
import type { FileWithDiagnostics } from './diagnostics-manager';
import '../util/polyfills';
type AnalyseServiceOptions = {
    puyaPath: string;
};
export type OpenDocuments = Record<string, {
    contents: string;
    version: number;
}>;
type AnalyseArgs = {
    uris: lsp.URI[];
    openDocuments: OpenDocuments;
};
export interface AnalyserService {
    analyse(options: AnalyseArgs, abort: AbortSignal): Promise<FileWithDiagnostics[]>;
    shutdown(): Promise<void>;
}
export declare function createAnalyserService(options: AnalyseServiceOptions): AnalyserService;
export {};
