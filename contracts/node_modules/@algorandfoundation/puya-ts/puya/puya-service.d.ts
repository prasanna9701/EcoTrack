import type { RootNode } from '../awst/nodes';
import type { SourceLocation } from '../awst/source-location';
import type { PuyaOptions } from '../options';
import type { AbsolutePath } from '../util/absolute-path';
interface CompileParams {
    awst: RootNode[];
    options: PuyaOptions;
    base_path: string;
    log_level: string;
    source_annotations: Record<string, string[]>;
}
interface Log {
    level: string;
    message: string;
    location?: SourceLocation;
}
interface LogResult {
    logs: Log[];
}
export type PuyaServiceOptions = {
    puyaPath: string;
};
export declare class PuyaService {
    private readonly connection;
    constructor(options: PuyaServiceOptions);
    analyse(programDirectory: AbsolutePath, awst: RootNode[]): Promise<LogResult>;
    compile(params: CompileParams): Promise<LogResult>;
    shutdown(): Promise<void>;
}
export {};
