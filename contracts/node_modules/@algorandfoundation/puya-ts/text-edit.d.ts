import type ts from 'typescript';
export type Position = {
    line: number;
    col: number;
};
export type Range = {
    start: Position;
    end: Position;
};
export type TextEdit = {
    range: Range;
    newText: string;
};
export declare function getNodeRange(node: ts.Node): Range;
