import type { InstanceBuilder, NodeBuilder } from '../index';
export declare const StaticIterator: unique symbol;
export interface StaticallyIterable {
    [StaticIterator](): InstanceBuilder[];
}
export declare function isStaticallyIterable<T extends NodeBuilder>(builder: T): builder is T & StaticallyIterable;
