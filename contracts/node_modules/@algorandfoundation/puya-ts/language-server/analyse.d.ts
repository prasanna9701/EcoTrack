import { CompileOptions } from '../options';
import type { DependencyGraph } from '../parser/dependency-graph';
import type { PuyaService } from '../puya/puya-service';
export declare function analyse({ abortSignal, puyaService, ...options }: Pick<CompileOptions, 'filePaths' | 'sourceFileProvider'> & {
    abortSignal?: AbortSignal;
    puyaService: PuyaService;
}): Promise<{
    graph?: DependencyGraph;
    outcome: 'invalid-typescript' | 'invalid-puya-ts' | 'invalid-puya' | 'valid';
}>;
