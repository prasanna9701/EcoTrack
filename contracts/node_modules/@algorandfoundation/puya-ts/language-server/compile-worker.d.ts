import type { TextDocument } from 'vscode-languageserver-textdocument';
import type { TextDocuments } from 'vscode-languageserver/node.js';
import type { AnalyserService } from './analyser-service';
import type { CompileTrigger, CompileTriggerQueue } from './compile-trigger-queue';
import type { DiagnosticsManager } from './diagnostics-manager';
export declare class CompileWorker {
    private readonly queue;
    private readonly analyserService;
    private readonly documents;
    private readonly diagnostics;
    stopping: boolean;
    private stopped;
    private sleeping;
    private activeBuild;
    constructor(queue: CompileTriggerQueue, analyserService: AnalyserService, documents: TextDocuments<TextDocument>, diagnostics: DiagnosticsManager);
    start(): void;
    stop(): Promise<void>;
    onCompileTriggerQueueItemQueued(trigger: CompileTrigger): void;
    private compileWithService;
}
