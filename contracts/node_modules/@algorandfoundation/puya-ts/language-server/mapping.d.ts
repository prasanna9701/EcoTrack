import type * as lsp from 'vscode-languageserver';
import type { SourceLocation } from '../awst/source-location';
import { type LogEvent } from '../logger';
export type CodeFixData = {
    type: 'code-fix';
    message: string;
    edits: readonly lsp.TextEdit[];
};
export declare function isCodeFixData(data: unknown): data is CodeFixData;
export type LogEventWithSource = LogEvent & {
    sourceLocation: SourceLocation & {
        file: string;
    };
};
export declare const mapper: {
    logToDiagnostic(event: LogEventWithSource): lsp.Diagnostic;
};
