export declare enum VersionCompareVerdict {
    ExactMatch = "ExactMatch",
    Inconclusive = "Inconclusive",
    MajorMismatch = "MajorMismatch",
    MinorMismatch = "MinorMismatch",
    OlderRevision = "OlderRevision",
    NewerRevision = "NewerRevision"
}
export declare function comparePuyaVersion(puyaPath: string): Promise<{
    target: string;
    found?: string;
    verdict: VersionCompareVerdict;
}>;
export declare function checkPuyaVersion(puyaPath: string): Promise<void>;
