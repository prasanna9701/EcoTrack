import type ts from 'typescript';
import type { Expression } from '../../../awst/nodes';
import type { SourceLocation } from '../../../awst/source-location';
import type { PType } from '../../ptypes';
import type { NodeBuilder } from '../index';
import { FunctionBuilder } from '../index';
export declare class AtFunctionBuilder extends FunctionBuilder {
    private expr;
    private itemPType;
    private exprLength;
    private returnsOptional;
    constructor(expr: Expression, itemPType: PType, exprLength: Expression | bigint, sourceLocation: SourceLocation, returnsOptional?: boolean);
    call(args: ReadonlyArray<NodeBuilder>, typeArgs: ReadonlyArray<PType>, sourceLocation: SourceLocation<ts.CallExpression>): NodeBuilder;
}
